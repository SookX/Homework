/*
Направете програма, която да принтира на екрана помощно меню, което да дава информация за това как се работи с
 приложението, което ще реализирате с това домашно. Когато програмата се стартира трябва на екрана да се покажат
  имената на три функции “strlength, strconcat и strcompare и срещу всяка една от тях информация какво прави тя. 
  Първата функция strlength трябва да намира дължината на символен низ. Втората функция strconcat трябва да 
  обединява два символни низа. Третата функция strcompare трябва да сравнява два символни низа. Направете Makefile
   който да се състои от две цели: “help.out” (под windows help.exe”), която да построява приложението и от целта 
   “clean”, която да изчиства файловете получени по време на компилация на приложението.
*/


/*
Направете заглавен файл “mystrings.h”, който да декларира 3 функции, които да работят със символни низове. 
Първата функция (strlength) трябва да намира дължината на символен низ, втората (strconcat) да слива два символни
 низа в първия подаден като аргумент на функцията, третата функция (strcompare) трябва да сравнява лексикално два 
 символни низа (да връща -1 ако първия стринг се намира лексикално преди втория, 0 - ако двата символни низа за 
 еднакви и 1 ако първия символен низ се намира след втория). Пример за strlength: Вход: “Ivan” => Изход: 4. 
 Пример за strconcat: Вход: str1=Hello, str2=World => strconcat(str1, str2) => Изход: str1=HelloWorld, str2=World.
  Пример за strcompare: Вход: “Alex”, “Alan” => Изход: 1, вход: “Alex”, “Alex” => Изход: 0, вход “Alex”, “Boyan” 
  => Изход: -1. Направете сорс файл mystrings.c, който да вмъква заглавния файл “mystrings.h” и да дефинира 
  функциите (да ги имплементира). Добавете в Makefile-a от първа задача цел, която се казва mystrings.o която 
  да компилира модула до обектен файл. Променете clean целта така че да изчиства и обектните файлове.
*/

/*

Направете три сорс файла “strlength.c”, “strconcat.c”, “strcompare.c”, които да демонстрират работата на трите
 функции от втора задача. Трите програми трябва да приемат аргументи от терминала за да работят и трябва да се
  компилират до изпълними файлове: strlength.c -> strlength.out (strlength.exe под Windows), strconcat.c -> 
  strconcat.out (strconcat.exe под Windows) и strcompare.c -> strcompare.out(strcompare.exe под Windows). 
  Примери за работата на трите програми изпълнени през терминала са: ./strlength.out Ivan => 4; ./strconcat.out 
  Hello World => HelloWorld, ./strcompare Alex Boyan =>-1. Трите програми трябва да използват mystrings.о модула
   за да работят. Тъй като програмата strconcat.out трябва да обединява два символни низа в първия, приемете, че
    големината на всеки един от двата символни низа (които се обединяват) не трябва да е повече от 50 символа. 
    Добавете в Makefile-а три цели (strlength.out, strconcat.out и strcompare.out - под windows вместо “out” 
    трябва да използвате “exe”), които да построяват трите програми. Направете цел “all”, която да поставите 
    като първа в Makefile-a. Нека тази цел да не прави нищо, а само да зависи на целите help.out, strlength.out,
     strconcat.out и strcompare.out за да може при изпълнение на make да се построят автоматично всички програми
      на приложението.

*/
#include <string.h>
#include <stdio.h>

#define CONCAT()


int main() {
    printf("\n Welocome to my app! \n Here are some functions: \n strlenght => returns the lenght of a char array \n strconcat => concats the two arrays\n strcompare => compares the two arrays");

    
}